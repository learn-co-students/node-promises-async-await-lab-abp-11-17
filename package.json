{
  "name": "node-module-exports-lab",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "mocha --timeout 10000 -R mocha-multi --reporter-options spec=-,json=.results.json"
  },
  "dependencies": {
    "debug": "~2.6.9",
    "mocha-multi": "^0.11.1",
    "pryjs": "^1.0.3"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "mocha": "^4.0.1"
  }
}
